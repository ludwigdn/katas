// Roman Numerals Kata
//
// I = 1, V = 5, X = 10, XL = 40,
// L = 50, C = 100, D = 500 et M = 1000

export const roman = (arabicNumber: number): string => {
  // Code here
  return "I".repeat(arabicNumber);
};
